# 新增 API 接口需求

> 访问域名：http://www.mcptools.xin
> 服务端口：8000

---

## 通用设计原则

### 数据存储策略
- **运行时数据**：所有数据优先存放在**内存（变量）**中，以加快请求响应速度
- **持久化备份**：收盘后将数据写入文件备份，存放在 `data/` 目录下
- **数据恢复**：服务启动时从文件加载历史数据到内存

### 数据目录结构
```
data/
├── stock_list/           # 股票列表数据
│   └── stock_info_a_code_name.csv
├── kline_daily/          # 历史日K线数据（按股票代码命名）
│   ├── 000001.csv
│   ├── 000002.csv
│   └── ...
├── kline_realtime/       # 当日分时数据（按日期/股票代码）
│   ├── 000001.csv
│   ├── 000002.csv
│   └── ...
├── fund_flow/            # 资金流向数据
│   └── fund_flow_latest.csv
└── stock_changes/        # 盘口异动数据
    └── stock_changes_latest.csv
```

### API 返回格式
- 所有接口返回 **DataFrame 格式**的 JSON 数据，保证可读性
- 返回格式示例：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "columns": ["日期", "开盘", "收盘", "最高", "最低", "成交量"],
    "index": [0, 1, 2, ...],
    "data": [
      ["2026-01-15", 10.5, 10.8, 11.0, 10.3, 1000000],
      ["2026-01-16", 10.8, 11.2, 11.5, 10.6, 1200000],
      ...
    ]
  }
}
```

---

## 1. 历史 K 线数据获取

### 功能说明
- 获取单只股票的历史 K 线数据（日K）
- 如果本地已存在数据，则不需要重复获取
- **返回数据包含历史日K线 + 当天K线**
- 当天K线是由第2点获取数据后，分拆得到，然后置入到对应内存变量中

### 数据源接口
```python
# 获取单只股票历史K线
stock_zh_a_hist_df = ak.stock_zh_a_hist(
    symbol="000001", 
    period="daily", 
    start_date="20170301", 
    end_date='20240528', 
    adjust="qfq"
)

# 获取所有股票列表（作为 symbol 参数来源）
stock_info_a_code_name = ak.stock_info_a_code_name()
```

### 数据更新策略
- `ak.stock_info_a_code_name()` 接口**每周调用一次**，更新股票列表
- 服务启动时，将股票列表读取到内存变量中
- 根据股票代码列表，读取**最近 90 个交易日**的日K线数据到内存
- **实时数据融合**：当请求历史K线时，将内存中的历史日K线与当天的分时数据合并
  - 当天K线 = 从当日分时数据中计算（开盘价、收盘价、最高价、最低价、成交量等）
- **收盘后备份**：收盘后将完整数据写入 `data/kline_daily/` 目录

### API 接口
- **请求方式**: GET
- **请求地址**: `http://www.mcptools.xin:8000/api/v1/data/kline?symbol=000001`
- **请求参数**: 
  - `symbol`: 股票代码
- **返回数据**: 最近 90 个交易日的日K线数据（含当天）

---

## 2. 实时分时数据获取

### 功能说明
- 获取股票当天的分时数据（1分钟线），（要等上一次获取结束再发起新的）
- 每只股票的数据存放在内存中，按股票代码索引
- 收盘后写入文件备份

### 数据源接口
```python
# 获取实时行情数据（全市场）
stock_zh_a_spot_em_df = ak.stock_zh_a_spot_em()
```

### 数据更新策略
- 根据 `ak.stock_info_a_code_name()` 接口获取的股票代码列表
- **开市时间内**每 **1 分钟**从 `ak.stock_zh_a_spot_em()` 获取最新数据
- **数据分拆处理**：
  1. 将获取的数据按股票代码拆分
  2. 每只股票的最新数据追加到内存中的分时数据列表
  3. 形成当天的 1 分钟K线数据（分时数据）
- **数据融合**：同时更新到历史K线的当天数据中（用于接口1的当天K线计算）
- **收盘后备份**：收盘后将分时数据写入 `data/kline_realtime/{日期}/` 目录

### API 接口
- **请求方式**: GET
- **请求地址**: `http://www.mcptools.xin:8000/api/v1/data/kline/real?symbol=000001`
- **请求参数**: 
  - `symbol`: 股票代码
- **返回数据**: 当天分时数据（1分钟K线）

---

## 3. 资金流向数据获取

### 功能说明
- 获取个股即时资金流向数据
- 每次获取时**覆盖**之前的数据

### 数据源接口
```python
# 获取个股资金流向（即时）
stock_fund_flow_individual_df = ak.stock_fund_flow_individual(symbol="即时")
```

### 数据更新策略
- **开市时间内**每 **5 分钟**读取一次数据
- 将数据**覆盖**缓存到内存变量中，按股票代码索引
- 当有请求时，根据请求的股票代码返回对应的数据
- 收盘后写入 `data/fund_flow/` 目录备份

### API 接口
- **请求方式**: GET
- **请求地址**: `http://www.mcptools.xin:8000/api/v1/data/fund-flow?symbol=000001`
- **请求参数**: 
  - `symbol`: 股票代码
- **返回数据**: 该股票的资金流向数据

---

## 4. 盘口异动

### 功能说明
- 获取盘口异动数据（东方财富-行情中心）
- 目标地址: http://quote.eastmoney.com/changes/
- 每次获取时**覆盖**之前的数据

### 数据源接口
```python
# 获取盘口异动数据
stock_changes_em_df = ak.stock_changes_em(symbol="大笔买入")
```

### 输入参数
| 名称 | 类型 | 描述 |
|------|------|------|
| symbol | str | 异动类型，可选值：`'火箭发射'`, `'快速反弹'`, `'大笔买入'`, `'打开跌停板'`, `'有大买盘'`, `'加速下跌'`, `'高台跳水'`, `'大笔卖出'`, `'打开涨停板'`, `'有大卖盘'` |

### 输出参数
| 名称 | 类型 | 描述 |
|------|------|------|
| 时间 | object | 异动发生时间 |
| 代码 | object | 股票代码 |
| 名称 | object | 股票名称 |
| 板块 | object | 异动类型 |
| 相关信息 | object | 注意: 不同的 symbol 的单位不同 |

### 数据示例
```
            时间   代码    名称    板块                       相关信息
0     14:55:51  872953  国子软件  大笔买入   124230,19.24000,0.300000
1     14:55:43  834021  流金科技  大笔买入    296001,4.52000,0.280453
2     14:55:28  301292  海科新源  大笔买入   142800,14.90000,0.119459
...        ...     ...   ...   ...                        ...
```

### 数据更新策略
- **开市时间内**每 **5 分钟**读取一次所有异动类型的数据
- 将数据**覆盖**缓存到内存变量中，按股票代码索引
- 当有请求时，根据请求的股票代码返回对应的异动数据
- 收盘后写入 `data/stock_changes/` 目录备份

### API 接口
- **请求方式**: GET
- **请求地址**: `http://www.mcptools.xin:8000/api/v1/data/stock-changes?symbol=000001`
- **请求参数**: 
  - `symbol`: 股票代码
- **返回数据**: 该股票当天的盘口异动数据

---

## API 接口汇总

| 序号 | 功能 | 请求地址 | 参数 | 更新频率 | 数据策略 |
|------|------|----------|------|----------|----------|
| 1 | 历史K线数据 | `/api/v1/data/kline` | symbol | 每日更新 | 累积追加 |
| 2 | 实时分时数据 | `/api/v1/data/kline/real` | symbol | 每1分钟 | 累积追加 |
| 3 | 资金流向 | `/api/v1/data/fund-flow` | symbol | 每5分钟 | 覆盖更新 |
| 4 | 盘口异动 | `/api/v1/data/stock-changes` | symbol | 每5分钟 | 覆盖更新 |

---

## 服务配置

### 端口配置
- **服务端口**: 8000
- **访问地址**: http://www.mcptools.xin:8000

### 定时任务
| 任务 | 执行时间 | 说明 |
|------|----------|------|
| 股票列表更新 | 每周一 9:00 | 更新 `ak.stock_info_a_code_name()` |
| 实时行情获取 | 开市时每1分钟 | 获取并分拆分时数据 |
| 资金流向获取 | 开市时每5分钟 | 覆盖更新资金流向数据 |
| 盘口异动获取 | 开市时每5分钟 | 覆盖更新盘口异动数据 |
| 数据备份 | 收盘后 15:30 | 将内存数据持久化到文件 |

### 开市时间
- 上午：9:15 - 11:30
- 下午：13:00 - 15:00
必需是交易日，同时在以上时间段，才启动定时读取